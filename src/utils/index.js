import { getValue, isValid } from './valueEvaluation';
import { generateHash, generateId } from './generateHash';
import * as str from './stringUtils';

/**
 * Barrel pattern just for convenience
 */
export {
  getValue,
  isValid,
  generateHash,
  generateId,
  str,
};
